# Local / secrets
.env
.DS_Store

# Leftover Python virtualenv / caches (from the former bot)
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Web tooling (when we add a build step)
node_modules/
dist/
.cache/
